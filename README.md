# ContextIQ AI
An operational risk and decision-intelligence platform built with FastAPI and SQLite.

## How to run locally
1. Activate virtual environment: `source backend/venv/bin/activate`
2. Install dependencies: `pip install -r backend/requirements.txt`
3. Run the server: `cd backend && uvicorn main:app --reload`
4. Run tests: `pytest -v`
